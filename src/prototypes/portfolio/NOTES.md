# Portfolio design lab

Prototype question: Which visual system best presents Idan's cloud architecture and backend profile while remaining clear, credible, and smooth at 1080p through 4K and on a Galaxy S23?

Variants:

- `terrain`: cinematic Three.js topology with an editorial content spine.
- `cinematic`: restrained liquid glass over a custom canvas signal field.
- `editorial`: bright architecture dossier with no continuous background animation.
- `console`: dense operational control plane with a lightweight orbital canvas.

Run with `npm run dev:lab`, then switch variants from the fixed comparison bar or the URL.

Verdict: pending review. Delete losing variants and rewrite the selected direction before merging anything into the production page.
